# Directory Structure Note

## Frontend Directory

The frontend application was previously located in the `apps/web/` directory.

The directory has now been renamed from `apps/` to `frontend/` to better reflect its purpose. The project structure is now more intuitive with the frontend application located at `frontend/web/`.

## Completed
- Renamed `apps/` directory to `frontend/`
- Updated all references to the old path in configuration files
- Updated documentation to reflect the new structure